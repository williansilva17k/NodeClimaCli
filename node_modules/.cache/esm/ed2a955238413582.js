let fetch,getApiToken;_384‍.x([["getCityID",()=>getCityID],["getCityForecastById",()=>getCityForecastById]]);_384‍.w("node-fetch",[["default",["fetch"],function(v){fetch=v}]]);_384‍.w("./apiToken",[["getApiToken",["getApiToken"],function(v){getApiToken=v}]]);



const apiUrl = 'http://apiadvisor.climatempo.com.br/api/v1'

async function getCityID(cityName) {

    try {
        const appToken = await getApiToken()
        const responseCity = await fetch(`${apiUrl}/locale/city?name=${encodeURI(cityName)}&token=${appToken}`)
        const cityJson = await responseCity.json()
    
        if (!cityJson.length) {
            if (cityJson.detail) {
                throw new Error(cityJson.detail)
            }
    
            throw new Error(`Cidade ${cityName} não encontrada.`)
            
        }
    
        return cityJson[0].id   
    } catch (error) {
        throw new Error(error)
    }
}

async function getCityForecastById(cityId) {

    try {
        const appToken = await getApiToken()
        const responseWeather = await fetch(`http://apiadvisor.climatempo.com.br/api/v1/locale/city?name=ituiutaba&token=${appToken}`)
        const weatherJson = await responseWeather.json()

        console.log(`weatherJson: ${JSON.stringify( weatherJson ) }`)

        return {
            name: weatherJson[0].name,
            state: weatherJson.state,
            country: weatherJson.country,
            ...weatherJson.data
        }
    } catch (error) {
        throw new Error(error)
    }
}




